ƒанный алгоритм состоит из двух циклов "For". ѕервый из них используетс€ дл€ подсчета количества строк в которых количество символов меньше или равно 3, а второй дл€ заполнени€ элементами итогового массива.
1. »нициализируем массив "myArr" в котором будут находитс€ строки;
2. »нициализируем переменную count, в которой будет хранитьс€ количество элементов длинна которых равна или меньше 3;
3. «апускаем цикл "for" который перебирает все элементы инициализированного массива;
4. ѕровер€ем длинну при помощи "if" если она меньше 4, то прибавл€ем к переменной count 1 если нет то ничего не делаем;
5. ƒалее создаем массив "resultArr" с длинной равной переменной count;
6. —оздаем переменную pos дл€ подсчета позиций элементов в массив "resultArr";
7. «апускаем цикл "for" который перебирает еще раз все элементы массива "myArr";
8. ѕровер€ем равна ли переменна€ "pos" длинне массива "resultArr" если да то завершаем цикл если нет то продолжаем выполнение цикла;
9. ѕрор€ем длинну элемента массива если меньше 4 то добавл€ем ее в массив "resultArr" и прибавл€ем еденицу к переемнной "pos" если нет то продолжаем работу цикла;
10. ¬ыводим массив при помощи форматировани€ строк и метода "Join" который ставит между элементами массива зап€тую с пробелом.
¬ результате выполнени€ нашей программы мы получаем массив в котором длинна всех элементов меньше или равна 3 и выводим его на консоль 